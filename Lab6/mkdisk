dd if=/dev/zero of=disk bs=1024 count=1024
mke2fs -b 1024 disk 1024
sudo mount disk /mnt
(sudo cd /mnt; sudo mkdir bin boot dev etc user)
sudo umount /mnt

(cd USER; ./mku u1; ./mku u2; ./mku init; ./mku sh)
echo done SDC disk
